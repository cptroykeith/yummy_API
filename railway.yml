version: 1
name: yummy_api

env:
  - key: PYTHON_VERSION
    value: "3.9.12"

build:
  - name: Install Dependencies
    command: pip install -r requirements.txt

services:
  - name: postgres

deploy:
  - name: Web
    command: python server.py
    port: 8000
