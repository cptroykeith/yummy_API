version: 1
name: yummy_api

env:
  - key: PYTHON_VERSION
    value: "3.8"

build:
  - name: Install Dependencies
    command: pip install -r requirements.txt

services:
  - name: postgres

deploy:
  - name: Web
    command: gunicorn app:app
    port: 8000
